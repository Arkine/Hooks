import styled from 'styled-components'

export const Home = styled.div`
	display: flex;

	position: relative;
	overflow: hidden;

	height: 100%;
	width: 100%;
	flex-grow: 1;
`

Home.Content = styled.div`
	display: flex;
	flex-flow: row wrap;

	flex-grow: 1;
`